/* Auth modal placeholder — reserved for future login / comunidade */
const auth = {
  init() {
    const modal = document.getElementById('auth-modal');
    if (!modal) return;
    // Keep empty for now; structure ready for future features
    modal.innerHTML = '';
  }
};

window.auth = auth;
